generator client {
  provider        = "prisma-client-js"
  output          = "../../../node_modules/.prisma/smk-client"
}

datasource db {
  provider = "postgresql"
  url      = env("SMK_DATABASE_URL")
}
